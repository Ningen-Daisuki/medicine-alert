import { Link } from "react-router-dom";

export const Lp = () =>{
    return(
        <dev>
            <Link to="/memberRegist">新規登録</Link>
            <Link to="/login">ログイン</Link>
        </dev>
    );
}